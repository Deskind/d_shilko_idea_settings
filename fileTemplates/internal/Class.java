#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
#parse("File Header.java")
/**
* @author: ${USER}
* Created: ${DATE}
* #if (${DESCRIPTION} != "") Class description: ${DESCRIPTION} #end
*/
    
public class ${NAME} {
    
}